# Copy to config.yaml and fill in. config.yaml is gitignored.

google_sheet_id: "PUT_SHEET_ID_HERE"        # the long id from the sheet URL
google_worksheet_name: "Sheet1"
google_credentials_file: "credentials.json"   # service account JSON key

openai_api_key: "sk-..."      # Whisper transcription; or set env OPENAI_API_KEY
gemini_api_key: "AIza..."     # question extraction; or set env GEMINI_API_KEY

transcription_model: "whisper-1"       # or "gpt-4o-transcribe"
extraction_model: "gemini-2.5-flash"   # or "gemini-2.5-pro"
extraction_max_tokens: 8000
